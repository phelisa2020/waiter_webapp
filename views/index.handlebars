<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width">
	<title>waiter</title>
	<link rel="stylesheet" href="css/waiter.css">
</head>
<body>
    <div class="container">
    <h1><u>Waiter Availability</u></h1>
			name

       <form action="/" method="post">
    <input type="text" name="waiterName" class="nameEntered" placeholder="enter your name">
			

			{{#if message}}
			<div class='message'>{{message}}</div>
			{{else}}
			<div class="info">
				<h1> {{messages.info}}</h1>
			</div>

			{{/if}}
						<button type="submit" class=" btn" value="Add names">Add</button>


		</form>
		
          
        </div>
   

			<div>
						<ul>
							{{#each days}}
							<li class="day">{{this.days}}</li>
							{{/each}}
						</ul>


					</div>
            
    </form>
  
    </div>
</body>

</html>